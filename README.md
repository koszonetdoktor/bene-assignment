## Start dev
- In the `ui` folder:  
```npm start```
- In the `server` folder:   
```npm run dev```
## Start production
1. Build the ui from the `ui` folder with:  
```npm run build```
1. From the root:   
```npm start```
